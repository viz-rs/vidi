[package]
name = "template-markup"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
viz.workspace = true

tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }

markup = "0.15"
v_htmlescape = "0.15"
