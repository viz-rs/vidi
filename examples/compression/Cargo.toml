[package]
name = "compression-example"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
vidi = { workspace = true, features = ["compression"] }

tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }
