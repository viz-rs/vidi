[package]
name = "csrf"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
viz = { path = "../../viz", features = ["csrf"] }

tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }
