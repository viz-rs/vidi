[package]
name = "session"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
viz = { path = "../../viz", features = ["session"] }
sessions = { version = "0.5.0", features = ["memory"] }

tokio = { version = "1.0", features = ["rt-multi-thread", "macros"] }
nano-id = "0.3"
